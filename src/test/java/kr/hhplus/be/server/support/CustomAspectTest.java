package kr.hhplus.be.server.support;

import org.junit.jupiter.api.Test;

class CustomAspectTest {

  // AOP는 무엇을 테스트해야 하는가?
  // 1. AOP가 적용된 메서드가 호출되었는가?
  // 2. AOP가 적용된 메서드가 호출되었을 때, AOP가 적용된 메서드가 호출되었는가?
  // 3. AOP의 적용 범위가 아닌 메서드가 호출되었을 때, AOP가 적용된 메서드가 호출되지 않았는가?
  @Test
  void methodCallTest() {
    // given
    // when
    // then
  }
}